services:
  trino:
    image: trinodb/trino:latest
    container_name: trino
    ports:
      - "8081:8080"
    volumes:
      - ./etc/catalog:/etc/trino/catalog:ro
    networks:
      - data-pipeline-network

networks:
  data-pipeline-network:
    external: true
